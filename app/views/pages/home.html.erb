<h1>Home</h1>

<%= image_tag current_user.profile_picture.url(:medium) %>
<% current_user.images.each do |img| %>
	<div><%= image_tag img.data.url(:thumb) %><br />
	<p><%= img.title %></p>
	</div>
<% end %>
